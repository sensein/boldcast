name: Deploy site to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'talks/**'
      - 'index.html'
      - 'mkdocs.yml'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one in-flight Pages deploy at a time; queue the next one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install MkDocs + theme
        run: pip install mkdocs mkdocs-material

      - name: Build /docs/ with MkDocs → site/docs/
        run: mkdocs build

      - name: Assemble final site (layer /talks/, /index.html, .nojekyll)
        run: |
          cp -r talks site/talks
          cp index.html site/index.html
          touch site/.nojekyll
          echo "--- site/ contents ---"
          ls -la site/
          echo "--- site/docs/ ---"
          ls site/docs/ | head -10
          echo "--- site/talks/ ---"
          ls site/talks/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
